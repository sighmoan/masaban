{
    "name": "masaban-board",
    "version": "0.0.1",
    "scripts": {
        "build-css": "sass --watch /src/index.scss /public",
        "serve": "http-server -p 8000",
        "dev": "npm-run-all --parallel build-css serve"
    },
    "dependencies": {
        "npm-run-all": "^4.1.5"
    }
}